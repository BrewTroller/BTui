//A data Store Used to hold System Settings
/***** Currently Not Used, Will be used when settings saving is implemented ********/
Ext.define('BTUI.store.BrewTroller', {
	extend: 'Ext.data.Store',
	fields: ['address']
});